#include "../../lib/tlpi_hdr.h"


extern char **environ;


int main(int argc, char *argv[])
{
	// for(; *environ != NULL; environ++) {
	// 	puts(*environ);
	// }

	// get an env variable
	char *e = getenv("_");

	// make some changes to it
	e[0] = 'W';
	e[1] = 'F';

	// demonstrate that it is the same string (static)
	printf("NEW _: %s\n", getenv("_"));

	// lets try to shove a string in putenv
	putenv("THIS=COOL");

	// it works i guess... not realistic to throw a string like that in putenv
	// unless it is generated by a macro
	printf("GETENV: %s\n", getenv("THIS"));

	setenv("WHAT", "TEST", 1);

	exit(EXIT_SUCCESS);
}
